# Force pnpm to behave as if in CI environment
# Prevents ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY in non-TTY environments
modules-cache-max-age=0
prefer-frozen-lockfile=false
auto-install-peers=true
shamefully-hoist=true
