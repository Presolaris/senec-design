---
// Exit-Intent-Popup: Erscheint wenn User die Seite verlassen will
---

<div id="exit-intent-popup" class="fixed inset-0 bg-black/50 backdrop-blur-sm z-50 hidden items-center justify-center p-4">
  <div class="bg-gradient-to-br from-[var(--senec-blue)] to-blue-900 rounded-2xl max-w-lg w-full relative shadow-2xl animate-scale-in">
    <!-- Close Button -->
    <button 
      id="close-exit-popup" 
      class="absolute top-4 right-4 text-white/80 hover:text-white transition-colors z-10"
      aria-label="Schließen"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
      </svg>
    </button>

    <!-- Content -->
    <div class="p-8 text-white">
      <!-- Icon & Title -->
      <div class="flex items-center gap-3 mb-4">
        <div class="w-12 h-12 bg-[var(--senec-yellow)] rounded-full flex items-center justify-center flex-shrink-0">
          <svg class="w-6 h-6 text-[var(--senec-blue)]" fill="currentColor" viewBox="0 0 20 20">
            <path d="M8 16.5a1.5 1.5 0 11-3 0 1.5 1.5 0 013 0zM15 16.5a1.5 1.5 0 11-3 0 1.5 1.5 0 013 0z"/>
            <path d="M3 4a1 1 0 00-1 1v10a1 1 0 001 1h1.05a2.5 2.5 0 014.9 0H10a1 1 0 001-1V5a1 1 0 00-1-1H3zM14 7a1 1 0 00-1 1v6.05A2.5 2.5 0 0115.95 16H17a1 1 0 001-1v-5a1 1 0 00-.293-.707l-2-2A1 1 0 0015 7h-1z"/>
          </svg>
        </div>
        <div>
          <p class="text-[var(--senec-yellow)] text-sm font-bold uppercase tracking-wide">Exklusives Angebot</p>
          <h2 class="text-3xl font-bold">Warten Sie! 🎁</h2>
        </div>
      </div>

      <p class="text-lg mb-6 leading-relaxed">
        Sichern Sie sich jetzt Ihre <strong class="text-[var(--senec-yellow)]">kostenlose Beratung</strong> und erhalten Sie ein unverbindliches Angebot innerhalb von 24 Stunden.
      </p>

      <!-- Benefits -->
      <div class="space-y-3 mb-6">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-[var(--senec-yellow)] rounded-lg flex items-center justify-center flex-shrink-0">
            <svg class="w-5 h-5 text-[var(--senec-blue)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
          </div>
          <p class="text-white/90"><strong>Bis zu 80%</strong> Stromkosten sparen</p>
        </div>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-[var(--senec-yellow)] rounded-lg flex items-center justify-center flex-shrink-0">
            <svg class="w-5 h-5 text-[var(--senec-blue)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <p class="text-white/90"><strong>0% MwSt.</strong> auf Solaranlagen 2026</p>
        </div>
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-[var(--senec-yellow)] rounded-lg flex items-center justify-center flex-shrink-0">
            <svg class="w-5 h-5 text-[var(--senec-blue)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
          <p class="text-white/90">Angebot in <strong>24 Stunden</strong></p>
        </div>
      </div>

      <!-- Form -->
      <form id="exit-intent-form" class="space-y-4">
        <!-- Email -->
        <div>
          <label for="exit-email" class="block text-sm font-medium mb-2">E-Mail oder Telefon *</label>
          <input 
            type="email" 
            id="exit-email" 
            name="email"
            placeholder="ihre@email.de"
            required
            class="w-full px-4 py-3 rounded-lg bg-white text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-[var(--senec-yellow)]"
          >
        </div>

        <!-- Phone -->
        <div>
          <label for="exit-phone" class="block text-sm font-medium mb-2">Oder Telefon für Rückruf</label>
          <input 
            type="tel" 
            id="exit-phone" 
            name="phone"
            placeholder="+49 123 456789"
            class="w-full px-4 py-3 rounded-lg bg-white text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-[var(--senec-yellow)]"
          >
        </div>

        <!-- Privacy Checkbox -->
        <div class="flex items-start gap-2">
          <input 
            type="checkbox" 
            id="exit-privacy" 
            name="privacy"
            required
            class="mt-1 w-4 h-4 rounded border-white/30 text-[var(--senec-yellow)] focus:ring-[var(--senec-yellow)]"
          >
          <label for="exit-privacy" class="text-sm text-white/80">
            Ich stimme der <a href="/datenschutz" class="underline hover:text-[var(--senec-yellow)]">Datenschutzerklärung</a> zu und willige ein, kontaktiert zu werden. *
          </label>
        </div>

        <!-- Submit Button -->
        <button 
          type="submit"
          class="w-full bg-[var(--senec-yellow)] text-[var(--senec-blue)] hover:bg-yellow-400 px-8 py-4 rounded-lg font-bold text-lg transition-all shadow-lg hover:shadow-xl"
        >
          Jetzt Angebot sichern
        </button>

        <p class="text-center text-sm text-white/60">
          Keine Sorge – wir rufen nur einmal an und nerven nicht.
        </p>
      </form>
    </div>
  </div>
</div>

<style>
  @keyframes scale-in {
    from {
      opacity: 0;
      transform: scale(0.9);
    }
    to {
      opacity: 1;
      transform: scale(1);
    }
  }

  .animate-scale-in {
    animation: scale-in 0.3s ease-out;
  }

  #exit-intent-popup.show {
    display: flex !important;
  }
</style>

<script>
  // Exit-Intent Detection
  let exitIntentShown = false;
  const popup = document.getElementById('exit-intent-popup');
  const closeBtn = document.getElementById('close-exit-popup');
  const form = document.getElementById('exit-intent-form');

  // Check if popup was already shown in this session
  if (sessionStorage.getItem('exitIntentShown')) {
    exitIntentShown = true;
  }

  // Detect mouse leaving viewport
  document.addEventListener('mouseleave', (e) => {
    // Only trigger if mouse leaves at the top (user wants to close tab/go back)
    if (e.clientY < 10 && !exitIntentShown) {
      showPopup();
    }
  });

  // Show popup after 30 seconds as fallback
  setTimeout(() => {
    if (!exitIntentShown) {
      showPopup();
    }
  }, 30000);

  function showPopup() {
    if (!popup) return;
    popup.classList.add('show');
    exitIntentShown = true;
    sessionStorage.setItem('exitIntentShown', 'true');
    document.body.style.overflow = 'hidden';
  }

  function hidePopup() {
    if (!popup) return;
    popup.classList.remove('show');
    document.body.style.overflow = '';
  }

  // Close button
  if (closeBtn) {
    closeBtn.addEventListener('click', hidePopup);
  }

  // Close on backdrop click
  if (popup) {
    popup.addEventListener('click', (e) => {
      if (e.target === popup) {
        hidePopup();
      }
    });
  }

  // Form submission
  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(form);
      const data = {
        email: formData.get('email'),
        phone: formData.get('phone'),
        source: 'Exit-Intent-Popup',
        access_key: '90f084c2-f6d4-4d7f-9b6b-c5e1b5e8b9e1'
      };

      try {
        const response = await fetch('https://api.web3forms.com/submit', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(data)
        });

        if (response.ok) {
          alert('Vielen Dank! Wir melden uns innerhalb von 24 Stunden bei Ihnen.');
          hidePopup();
          form.reset();
        } else {
          alert('Es gab ein Problem. Bitte versuchen Sie es erneut oder rufen Sie uns an: +49 341 98 99 03 91');
        }
      } catch (error) {
        console.error('Form submission error:', error);
        alert('Es gab ein Problem. Bitte versuchen Sie es erneut oder rufen Sie uns an: +49 341 98 99 03 91');
      }
    });
  }

  // ESC key to close
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && popup?.classList.contains('show')) {
      hidePopup();
    }
  });
</script>
